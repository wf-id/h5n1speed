# Format deterministic output

Format deterministic output

## Usage

``` r
format_det_ode_output(dat, states = compartment_defn$state)
```

## Arguments

- dat:

  a dataset generate from the function

- states:

  a vector representing the states used in the compartmental model

## Value

a data.frame formatted with the appropriate column names
